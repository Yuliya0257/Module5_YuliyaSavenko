numpy>=1.26
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
python-docx>=1.0
# scikit-learn and scipy are NOT required: code/ml_lib.py provides
# from-scratch NumPy implementations of the algorithms used.
#
# OPTIONAL install either to have the gradient-boosting model use an
# optimised, industry-standard library instead of the from-scratch
# implementation (run_analysis.py auto-detects and uses it):
# lightgbm>=4.0
# xgboost>=2.0
